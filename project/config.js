/**
 * Configurations
 */

var config = {
    // the character used to separate columns in CSV files
    csvDelimiter: "\t",

    // can either be \n or \r\n to separate lines in CSV files
    csvNewLine: "\n"
};

module.exports = config;